
const { createProxyMiddleware } = require('http-proxy-middleware');

const socketProxy = createProxyMiddleware('/items/websocket', {
    target: 'http://localhost:4000/items/websocket',
    ws: true,
});